# Bastion IDS - MINIMAL runtime dependencies for the shipped product.
# Versions pinned to match the trained models so the pickles load correctly.
# (Excludes all dev/experiment packages: streamlit, plotly, opencv, matplotlib,
#  pyinstaller, impacket, ldap3, GitPython, etc.)
fastapi==0.136.1
uvicorn==0.47.0
python-multipart==0.0.29
numpy==2.1.3
pandas==2.3.3
scipy==1.16.3
scikit-learn==1.6.1
xgboost==3.2.0
catboost==1.2.10
joblib==1.5.2
tensorflow==2.21.0
scapy==2.6.1
psutil==7.2.2
reportlab
